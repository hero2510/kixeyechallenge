pipeline {
    agent any

    stages {
        stage('Hello') {
            
            steps {
                echo GIT_BRANCH
                echo GIT_COMMIT
                echo 'Hello World'
            }
        }
    }
}
